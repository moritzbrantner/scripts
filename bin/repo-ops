#!/usr/bin/env python3
from pathlib import Path
import sys
sys.path.insert(0,str(Path(__file__).resolve().parents[1]))
from repo_ops import COMMANDS, main
invoked=Path(sys.argv[0]).name
raise SystemExit(main([invoked,*sys.argv[1:]] if invoked in COMMANDS else sys.argv[1:]))
